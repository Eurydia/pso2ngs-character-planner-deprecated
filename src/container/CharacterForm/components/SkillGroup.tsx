import { Grid } from "@mui/material";
import { FC, Fragment } from "react";

interface SkillGroupProps {}
const SkillGroup: FC<SkillGroupProps> = (props) => {
  return <Grid container spacing={2}></Grid>;
};

export default SkillGroup;
